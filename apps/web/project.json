{
  "name": "web",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/web/src",
  "projectType": "application",
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "options": {
        "command": "vite",
        "cwd": "{projectRoot}"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "options": {
        "commands": [
          "vite build"
        ],
        "cwd": "{projectRoot}",
        "nodeOptions": ["--max-old-space-size=8192"]
      },
      "inputs": [
        "production",
        "^production",
        "{workspaceRoot}/vite.config.ts",
        "{projectRoot}/vite.config.ts",
        "{projectRoot}/index.html",
        "{projectRoot}/public/**/*"
      ],
      "outputs": [
        "{projectRoot}/dist"
      ],
      "cache": true
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit",
        "cwd": "{projectRoot}",
        "nodeOptions": ["--max-old-space-size=8192"]
      },
      "cache": true
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint . --max-warnings=100",
        "cwd": "{projectRoot}"
      },
      "cache": true
    },
    "lint:fix": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint . --fix",
        "cwd": "{projectRoot}"
      },
      "cache": true
    },
    "test": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "options": {
        "command": "vitest run --project=unit --project=component --coverage",
        "cwd": "{projectRoot}",
        "nodeOptions": ["--max-old-space-size=8192"]
      },
      "inputs": [
        "default",
        "^production",
        "{workspaceRoot}/vitest.workspace.ts",
        "{projectRoot}/vitest.config.ts"
      ],
      "outputs": [
        "{projectRoot}/coverage"
      ],
      "cache": true
    },
    "test:unit": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "options": {
        "command": "vitest run --project=unit --coverage",
        "cwd": "{projectRoot}",
        "nodeOptions": ["--max-old-space-size=8192"]
      },
      "inputs": [
        "default",
        "^production",
        "{workspaceRoot}/vitest.workspace.ts",
        "{projectRoot}/vitest.config.ts"
      ],
      "outputs": [
        "{projectRoot}/coverage"
      ],
      "cache": true
    },
    "test:component": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "options": {
        "command": "vitest run --project=component",
        "cwd": "{projectRoot}"
      },
      "cache": true
    },
    "test:integration": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "options": {
        "command": "vitest run --project=integration",
        "cwd": "{projectRoot}",
        "nodeOptions": ["--max-old-space-size=8192"]
      },
      "inputs": [
        "default",
        "^production",
        "{workspaceRoot}/vitest.workspace.ts",
        "{projectRoot}/vitest.config.ts"
      ],
      "outputs": [
        "{projectRoot}/coverage"
      ],
      "cache": true
    },
    "test:e2e": {
      "executor": "nx:noop",
      "options": {},
      "cache": false
    },
    "test:watch": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "options": {
        "command": "vitest --watch --project=unit --project=component --coverage",
        "cwd": "{projectRoot}"
      }
    },
    "preview": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "vite preview",
        "cwd": "{projectRoot}"
      },
      "cache": true
    },
    "verify": {
      "executor": "nx:run-commands",
      "dependsOn": ["typecheck", "lint", "test", "build"],
      "options": {
        "commands": [
          "echo '✅ TypeScript checking passed'",
          "echo '✅ Linting passed'",
          "echo '✅ Tests passed'",
          "echo '✅ Build completed successfully'",
          "echo '🎉 All quality checks passed!'"
        ],
        "parallel": false,
        "cwd": "{projectRoot}"
      }
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rm -rf dist coverage .nx/cache .tanstack node_modules/.vite",
        "cwd": "{projectRoot}"
      },
      "cache": false
    }
  },
  "tags": ["type:app", "scope:web", "layer:app"]
}
