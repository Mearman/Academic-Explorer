{
  "name": "academic-explorer",
  "$schema": "./node_modules/nx/schemas/project-schema.json",
  "sourceRoot": ".",
  "projectType": "application",
  "targets": {
    "syncpack": {
      "executor": "nx:run-commands",
      "options": {
        "command": "syncpack"
      },
      "cache": false
    },
    "syncpack:fix": {
      "executor": "nx:run-commands",
      "options": {
        "command": "syncpack fix-mismatches"
      },
      "inputs": [
        "{workspaceRoot}/**/package.json",
        "{workspaceRoot}/syncpack.config.ts"
      ],
      "outputs": [
        "{workspaceRoot}/**/package.json"
      ],
      "cache": false
    },
    "syncpack:lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "syncpack lint"
      },
      "inputs": [
        "{workspaceRoot}/**/package.json",
        "{workspaceRoot}/syncpack.config.ts",
        "{workspaceRoot}/pnpm-lock.yaml"
      ],
      "outputs": [],
      "cache": true
    },
    "syncpack:list": {
      "executor": "nx:run-commands",
      "options": {
        "command": "syncpack list-mismatches"
      },
      "inputs": [
        "{workspaceRoot}/**/package.json",
        "{workspaceRoot}/syncpack.config.ts"
      ],
      "outputs": [],
      "cache": true
    },
    "deps:audit": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm audit --audit-level moderate"
      },
      "inputs": [
        "{workspaceRoot}/pnpm-lock.yaml"
      ],
      "outputs": [],
      "cache": true
    },
    "deps:check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsx tools/scripts/check-dependencies.ts",
        "cwd": "{workspaceRoot}"
      },
      "inputs": ["default", "^production"],
      "outputs": ["{workspaceRoot}/dependency-report.json"]
    },
    "docs:task-graph": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsx scripts/generate-nx-mermaid.ts -u README.md -t build,test,typecheck,lint -d LR",
        "cwd": "{workspaceRoot}"
      },
      "inputs": [
        "{workspaceRoot}/README.md",
        "{workspaceRoot}/nx.json",
        "{workspaceRoot}/project.json",
        "{workspaceRoot}/apps/*/project.json",
        "{workspaceRoot}/packages/*/project.json",
        "{workspaceRoot}/scripts/generate-nx-mermaid.ts"
      ],
      "outputs": [
        "{workspaceRoot}/README.md"
      ],
      "cache": true
    },
    "build": {
      "executor": "nx:noop",
      "dependsOn": [
        "docs:task-graph"
      ],
      "cache": true
    }
  },
  "tags": ["scope:workspace", "type:root"]
}