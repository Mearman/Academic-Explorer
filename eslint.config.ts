// Main ESLint configuration for Academic Explorer monorepo
// This extends the base configuration for workspace-level linting
import baseConfig from "./eslint.config.base.ts";

export default baseConfig;